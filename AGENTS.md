# AGENTS.md

## Agent skills

### Issue tracker

Issues live as GitHub issues in `aAtila/slusko`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical role names — `needs-triage`, `needs-info`, `blocked`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
